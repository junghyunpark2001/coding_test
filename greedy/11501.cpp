#include <iostream>

using namespace std;

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t,n;
    cin>>t>>n;
    for(int i=0;i<t;i++){
        for(int j=0;j<n;i++){
            
        }
    }
}